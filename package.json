{
  "name": "vevurn-pos-system",
  "version": "1.0.0",
  "description": "Vevurn POS System - Complete monorepo with separate backend and frontend",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "workspaces": [
    "backend/shared",
    "backend", 
    "frontend"
  ],
  "scripts": {
    "// Development": "# Development commands for running services",
    "dev": "turbo run dev --parallel",
    "backend:dev": "pnpm --filter @vevurn/backend dev",
    "frontend:dev": "pnpm --filter frontend dev",

    "// Building": "# Build commands for all packages",
    "build": "turbo run build",
    "backend:build": "pnpm --filter @vevurn/shared build && pnpm --filter @vevurn/backend build",
    "frontend:build": "pnpm --filter @vevurn/shared build && pnpm --filter frontend build",

    "// Testing": "# Testing commands for all packages",
    "test": "turbo run test",
    "test:unit": "turbo run test:unit",
    "test:integration": "turbo run test:integration",
    "test:e2e": "turbo run test:e2e --concurrency=1",
    "backend:test": "pnpm --filter @vevurn/backend test",
    "frontend:test": "pnpm --filter frontend test",

    "// Database": "# Database management commands",
    "db:migrate": "pnpm --filter @vevurn/backend db:migrate",
    "db:seed": "pnpm --filter @vevurn/backend db:seed",
    "db:studio": "pnpm --filter @vevurn/backend db:studio",
    "db:generate": "pnpm --filter @vevurn/backend db:generate",
    "db:reset": "pnpm --filter @vevurn/backend db:reset",
    "db:backup": "bash backend/scripts/database/backup.sh",
    "db:restore": "bash backend/scripts/database/restore.sh",

    "// Docker": "# Docker and containerization commands",
    "docker:up": "cd backend && docker-compose up -d",
    "docker:build": "cd backend && docker-compose build",
    "docker:down": "cd backend && docker-compose down",
    "docker:logs": "cd backend && docker-compose logs -f",
    "docker:clean": "docker system prune -f",
    "monitoring:up": "cd backend && docker-compose -f docker-compose.prod.yml up -d prometheus grafana loki",
    "monitoring:down": "cd backend && docker-compose -f docker-compose.prod.yml down",

    "// Utility": "# Utility and maintenance commands",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "clean": "turbo run clean && rm -rf node_modules",
    "reset": "pnpm clean && pnpm install",
    "shared:build": "pnpm --filter @vevurn/shared build",
    "shared:test": "pnpm --filter @vevurn/shared test",
    "backend:start": "pnpm --filter @vevurn/backend start",
    "frontend:start": "pnpm --filter frontend start",
    "security:audit": "pnpm audit --audit-level high",
    "security:scan": "bash backend/scripts/utilities/security-scan.sh",
    "health-check": "bash backend/scripts/maintenance/health-check.sh",
    "performance-test": "bash backend/scripts/utilities/performance-test.sh"
  },
  "devDependencies": {
    "@types/node": "^24.2.0",
    "@typescript-eslint/eslint-plugin": "^8.39.0",
    "@typescript-eslint/parser": "^8.39.0",
    "eslint": "^9.32.0",
    "eslint-config-prettier": "^10.1.8",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.4",
    "prettier": "^3.6.2",
    "turbo": "^2.5.5",
    "typescript": "^5.9.2"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md,yml,yaml}": [
      "prettier --write"
    ]
  }
}
